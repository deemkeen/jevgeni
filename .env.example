# Copy to .env (git-ignored).
GROQ_API_KEY=            # Whisper Large v3 Turbo via Groq
TYPESAFE_API_KEY=        # Jev (typesafe.ai)
JEV_MODEL=jev-1.13.0     # pinned model version
SPEND_LIMIT_USD=0.10     # hard daily cap for Jev spend (server refuses above)
JEVGENI_PORT=8787
JEVGENI_TOKEN=          # optional: if set, the web app needs ?token=... once (stored in sessionStorage)
